controle_motor folder contain a python code to manualy control motors via socket_client server script and autonomously from the nav2 vel_command (the code is using rgpio library to control rasp_pins)
